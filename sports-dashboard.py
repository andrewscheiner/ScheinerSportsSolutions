import streamlit as st

st.set_page_config(
    page_title="Andrew Scheiner's Sports Dashboard",
    page_icon=":trophy:",
    layout="wide",
    initial_sidebar_state="expanded")

st.set_page_config(page_title="Andrew Scheiner's Sports Dashboard", page_icon=":trophy:", layout="wide")

st.title('Scheiner Sports Solutions')
st.markdown('An interactive Streamlit dashboard containing multiple sports solutions \
         for fantasy and betting help. Designed by Andrew Scheiner.')

import streamlit as st

st.title("My Streamlit Homepage")

# -------------------------------
# Betting Systems Section (3x1)
# -------------------------------
st.header("Betting Systems")

betting_tools = [
    ("⚾ Pitcher Props", "pages/1_PitcherProps.py"),
    ("🪜 Laddering Tool", "pages/3_LadderingTool.py"),
    ("🏀 NBA Betting Systems", "pages/4_NBABettingSystems.py")
]

cols = st.columns(3)
for i, (label, target) in enumerate(betting_tools):
    if cols[i].button(label, key=f"bet_{i}"):
        st.switch_page(target)

# -------------------------------
# Seasonal Tools Section (2x2)
# -------------------------------
st.header("Seasonal Tools")

seasonal_tools = [
    ("🔍 Tango Tracker", "pages/2_TangoTracker.py"),
    ("🧊 Slump Detector", "pages/6_SlumpDetector.py"),
    ("🏈 NFL Power Rankings", "pages/5_NFLPowerRankings.py"),
    ("Reverse Run Your Pool", "pages/7_ReverseRunYourPool.py"),
]

cols = st.columns(2)
for i, (label, target) in enumerate(seasonal_tools):
    col = cols[i % 2]
    if col.button(label, key=f"season_{i}"):
        st.switch_page(target)

    if (i + 1) % 2 == 0 and i + 1 < len(seasonal_tools):
        cols = st.columns(2)
        
st.write("Data sources and packages: Fangraphs, pybaseball, nfl-data-py, MLB Stats API, Kaggle")
st.write("Disclaimer: I will not be displaying any lines posted by sportsbooks, and all predictions generated by my models are only recommendations. Please bet responsibly and only risk what you are willing to lose.")
st.write("© Andrew Scheiner 2025")